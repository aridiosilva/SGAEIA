# Integration Deployment Profiles

Deployment profiles select one concrete adapter per Integration Port for a deployment context. They do not modify the Integration Port semantics.

A profile is admissible only if every selected adapter meets the minimum conformance level declared by `specs/integrations/conformance-matrix.csv` for the target environment.

Profiles are examples, not product endorsements or external certifications.
